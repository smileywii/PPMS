package ppms.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import ppms.domain.TrainingMethod;

public interface TrainingMethodRepository extends JpaRepository<TrainingMethod, Long> {

}
